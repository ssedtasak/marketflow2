{
  "name": "marketflow2",
  "version": "0.1.0",
  "private": true,
  "description": "MarketFlow — Cloudflare-native project management for marketing teams",
  "scripts": {
    "dev:web": "pnpm --filter @marketflow/web dev",
    "dev:api": "pnpm --filter @marketflow/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
